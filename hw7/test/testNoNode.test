# Find a path with Dijkstra's algorithm when one of the nodes
# does not exist

CreateGraph graph

AddNode graph a
AddNode graph b
AddNode graph c

AddEdge graph a b 1
AddEdge graph b a 1
AddEdge graph b c 1
AddEdge graph c b 1

FindPath graph d c
FindPath graph c d
