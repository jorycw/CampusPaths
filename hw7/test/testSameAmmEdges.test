# Find a path with Dijkstra's algorithm when there are two distinct paths
# in which both contain two edges, but one has a lower cost

CreateGraph graph
AddNode graph a
AddNode graph b
AddNode graph c
AddNode graph d

AddEdge graph a b .5
AddEdge graph b d 3.6

AddEdge graph a c 2
AddEdge graph c d 2

FindPath graph a d