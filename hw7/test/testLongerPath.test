# Find a path with Dijkstra's algorithm when there are two distinct paths
# in which the path containing more edges has a lower cost

CreateGraph graph
AddNode graph a
AddNode graph b
AddNode graph c
AddNode graph d

AddNode graph a.5

AddEdge graph a a.5 .2
AddEdge graph a.5 b .1
AddEdge graph b d 2.2

AddEdge graph a c 2
AddEdge graph c d 2

FindPath graph a d