# Find a path with Dijkstra's algorithm when the path s
# between the two nodes are not the correct direction

CreateGraph graph

AddNode graph a
AddNode graph b
AddNode graph c

AddEdge graph c b 1
AddEdge graph b a 1

FindPath graph a c